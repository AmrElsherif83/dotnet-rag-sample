# ===========================================
# Environment Configuration Template
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to source control!
# ===========================================

# OpenAI Configuration (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# PostgreSQL Configuration (optional in development)
# These have sensible defaults for local development with docker-compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ragdb
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Alternatively, use a full connection string:
# POSTGRES_CONNECTION_STRING=Host=localhost;Port=5432;Database=ragdb;Username=postgres;Password=postgres

# API Key Authentication (OPTIONAL)
# Set this to enable API key authentication
# All requests will require X-API-KEY header
# API_KEY=your-secret-api-key-here
